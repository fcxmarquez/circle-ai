NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
DATABASE_URL=
ENABLE_DEV_TOOLS=false

# Optional provider API keys.
# When set, these override any keys configured in the app UI and the matching
# input is locked. Useful for self-hosted deployments where end users should
# share a single key.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
